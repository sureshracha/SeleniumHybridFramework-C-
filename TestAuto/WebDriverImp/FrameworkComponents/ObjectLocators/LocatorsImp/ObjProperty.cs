namespace TestAutomation.WebComponents.ObjectProperties
{
    /// <summary>
    /// Generalise get feature to get the elements(Objects) property value
    /// </summary>
    
    public enum GetBy
        {
            id,
            text,
            classname,
            name,
            tagname,
            value,
            innertext,
            outertext,
            innerhtml,
            outerhtml,
            href,
            src,
            type,
            title,
            maxLength
        }
    /// <summary>
    /// Generalise find feature to locate the elements(Objects)
    /// </summary>
    
    public enum FindBy
        {
            id,
            classname,
            name,
            cssselector,
            linktext,
            partiallinktext,
            tagname,
            xpath,
            title,
            innertext,
            outertext,
            innerhtml,
            outerhtml,
            index
        }

   

   

}
